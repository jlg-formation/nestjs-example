id: nestjs-backend-3j-05-process-controller-test
type: process
title: "Étapes : tester un contrôleur NestJS"
subtitle: "Setup → action → assertions"

layout:
  template: process
  density: medium
  emphasis: balanced

visual:
  kind: diagram
  query: "testing steps setup act assert"
  source: ai
  placement: right
  alt: "Étapes test"

process:
  steps:
    - "Créer un mock du service (useValue)"
    - "Compiler un TestingModule"
    - "Récupérer le controller"
    - "Appeler la méthode"
    - "Vérifier appels + réponse"

covers:
  - "Test des contrôleurs"

notes_formateur: |
  ## Déroulé (2 min)

  ### Points clés
  - Test simple = confiance.
  - Vérifier le strict minimum : contrat et délégation.

  ### Transition
  Maintenant, on teste l'API 'pour de vrai' : e2e.

notes_stagiaire: |
  ## À retenir

  **Étapes** : mock → module → controller → call → assert.
